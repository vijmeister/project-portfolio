The report is in file: _____

The preprocessing program is preprocessing.py. It generates new test and train tables that will be used in the classification program.

The classification program is classification 5.py. It takes the tables exported from the preprocessing program and indicates the accuracy score, confusion matrix, and classification report.

The log file is log.docx